'use client';

import styled from 'styled-components';

export const Hero = styled.section`
  padding: 3rem 1rem;
  position: relative;
  isolation: isolate;
  overflow: hidden;
  margin-bottom: 2rem;
`;

export const HeroText = styled.h1`
  font-size: 4rem;
  font-weight: 900;
  text-align: center;
`;
