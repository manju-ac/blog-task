'use client';

import { Skeleton } from '@/app/global.styled';
import styled from 'styled-components';

export const ViewCounterSkeleton = styled(Skeleton)`
  width: 5rem;
  height: 1.5rem;
  border-radius: var(--border-rad-md);
`;
